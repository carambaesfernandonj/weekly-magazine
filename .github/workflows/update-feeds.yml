name: Build WEEKLY issue

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * 0"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check Supabase secrets
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: |
          if [ -z "$SUPABASE_URL" ]; then
            echo "::error::SUPABASE_URL is not available to this workflow."
            exit 1
          fi

          if [ -z "$SUPABASE_SECRET_KEY" ]; then
            echo "::error::SUPABASE_SECRET_KEY is not available to this workflow."
            exit 1
          fi

          echo "SUPABASE_URL configured: YES"
          echo "SUPABASE_SECRET_KEY configured: YES"

      - name: Fetch RSS and Atom feeds
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python scripts_fetch_rss.py

      - name: Run AI editorial selection
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python scripts_editor.py

      - name: Publish issue data
        run: |
          git config user.name "weekly-editor"
          git config user.email "weekly-editor@users.noreply.github.com"
          git add data/articles.json data/editorial.json data/feeds.json
          git diff --cached --quiet || git commit -m "Publish WEEKLY issue"
          git push
